---
driver:
  name: vagrant

provisioner:
  name: chef_solo

platforms:
  - name: ubuntu-12.04
  - name: centos-6.4

suites:
  - name: default
    data_bags_path: "test/integration/data_bags"
    run_list:
      - recipe[csync2::default]
    attributes:
